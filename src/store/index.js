import { createStore } from 'vuex'
import tabsModule from './modules/livestockTabs'

export default createStore({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        tabs: tabsModule,
    }
})
